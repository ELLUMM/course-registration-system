spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/course_registration}
    username: ${DATABASE_USERNAME:minkyung}
    password: ${DATABASE_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

server:
  port: ${PORT:8080}

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:1234}